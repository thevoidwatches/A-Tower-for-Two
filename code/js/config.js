/* History Settings */
    //History Settings
        Config.history.controls = true;

/* Macro Settings */
    // Do not type on previously visited passages
        Config.macros.typeVisitedPassages = false;

/* Passages Settings */
    // Makes passages automatically be [nobr]
        Config.passages.nobr = true;

/* Saves Settings */
    // Autosaves on passages tagged with "autosave"
        Config.saves.autosave = ["autosave"];

    // Prompts the player about loading the autosave
        Config.saves.autoload = "prompt";

    // Gives extra save slots.
        Config.saves.slots = 12;